<h1>Articles</h1>
<p><%= link_to 'Add an article', new_article_url %></p>
<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_url(article) %> |
      <%= link_to 'Edit', edit_article_url(article) %>
    </li>
  <% end %>
</ul>